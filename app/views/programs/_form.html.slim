= form_for(program) do |f|
  = render "shared/field", f: f, field: :input, type: :text
  = render "schedules/nested", f: f
  = f.submit submit, class: "bg-black text-white px-4 py-2 rounded cursor-pointer"
