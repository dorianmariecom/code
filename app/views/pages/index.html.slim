.p= button_to t(".new"), new_page_path, method: :get, class: "bg-black text-white rounded px-4 py-2 cursor-pointer"

.p= button_to t(".delete_all"), pages_path, method: :delete, class: "a text-red-600" if @pages.any?

= paginate @pages
= render partial: "pages/page", collection: @pages
= paginate @pages
