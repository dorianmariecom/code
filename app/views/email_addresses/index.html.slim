.p= button_to t(".new"), new_url, method: :get, class: "bg-black text-white rounded px-4 py-2 cursor-pointer"

.p= button_to t(".delete_all"), url, method: :delete, class: "a text-red-600" if @email_addresses.any?

= paginate @email_addresses
= render partial: "email_addresses/email_address", collection: @email_addresses
= paginate @email_addresses
