= form_for user do |f|
  = render "shared/field", f: f, field: :time_zone, type: :time_zone, options: time_zone_options, required: true
  = render "email_addresses/nested", f: f
  = f.submit submit, class: "bg-black text-white px-4 py-2 rounded cursor-pointer"
