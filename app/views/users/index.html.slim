.p= button_to t(".new"), new_user_path, method: :get, class: "bg-black text-white rounded px-4 py-2 cursor-pointer"

.p= button_to t(".delete_all"), users_path, method: :delete, class: "a text-red-600" if @users.any?

= paginate @users
= render partial: "users/user", collection: @users
= paginate @users
