p= link_to t(".new"), new_url, class: "no-underline inline-block bg-black text-white rounded px-4 py-2 cursor-pointer"

.p= button_to t(".delete_all"), url, method: :delete, class: "a text-red-600" if @x_accounts.any?

= paginate @x_accounts
= render partial: "x_accounts/x_account", collection: @x_accounts
= paginate @x_accounts
