.p= button_to t(".new"), new_url, method: :get, class: "bg-black text-white rounded px-4 py-2 cursor-pointer"

.p= button_to t(".delete_all"), url, method: :delete, class: "a text-red-600" if @slack_accounts.any?

= paginate @slack_accounts
= render partial: "slack_accounts/slack_account", collection: @slack_accounts
= paginate @slack_accounts
