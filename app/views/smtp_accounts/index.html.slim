.p= button_to t(".new"), new_smtp_account_path, method: :get, class: "bg-black text-white rounded px-4 py-2 cursor-pointer"

.p= button_to t(".delete_all"), url, method: :delete, class: "a text-red-600" if @smtp_accounts.any?

= paginate @smtp_accounts
= render @smtp_accounts
= paginate @smtp_accounts
