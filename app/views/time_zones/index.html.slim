.p= button_to t(".new"), new_time_zone_path, method: :get, class: "bg-black text-white rounded px-4 py-2 cursor-pointer"

.p= button_to t(".delete_all"), url, method: :delete, class: "a text-red-600" if @time_zones.any?

= paginate @time_zones
= render partial: "time_zones/time_zone", collection: @time_zones
= paginate @time_zones
