---
env:
  secret:
    - RAILS_MASTER_KEY
    - HOST
    - HOSTS
service: code-staging
image: dorianmariecom/code-staging
servers:
  web:
    - 138.201.175.197
  jobs:
    cmd: bin/rake solid_queue:start
    hosts:
    - 138.201.175.197
  push:
    cmd: bin/rpush start -f
    hosts:
    - 138.201.175.197
proxy:
  ssl: true
  hosts:
    - staging.code-dorian.com
    - staging.code.dorianmarie.com
    - staging.codedorian.co
    - staging.codedorian.com
    - staging.codedorian.fr
    - staging.codedorian.net
    - staging.codedorian.org
registry:
  username:
    - KAMAL_REGISTRY_USERNAME
  password:
    - KAMAL_REGISTRY_PASSWORD
env:
  secret:
    - RAILS_MASTER_KEY
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole"
volumes:
  - "code_storage:/rails/storage"
builder:
  arch: arm64
