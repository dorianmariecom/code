dispatchers:
  - recurring_tasks:
      schedule_all_job:
        class: ScheduleAllJob
        schedule: every minute
      x_accounts_refresh_auth_job:
        class: XAccountsRefreshAuthJob
        schedule: every minute
